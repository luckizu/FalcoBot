package FalcoBot.Eventi;

import net.dv8tion.jda.api.events.message.guild.GuildMessageReceivedEvent;
import net.dv8tion.jda.api.events.message.guild.react.GenericGuildMessageReactionEvent;
import net.dv8tion.jda.api.events.message.guild.react.GuildMessageReactionAddEvent;
import net.dv8tion.jda.api.hooks.ListenerAdapter;

public class IconRecieved extends ListenerAdapter {
    public void onGuildMessageReceived(GuildMessageReceivedEvent e){
       /* String [] Messaggio = new String[100];
       String Messaggio = e.getMessage().getContentRaw();
        String  [] Comandi = {"ha", "richiesto", "di","collegarsi", "al", "canale", "I", "rapaci"};
        System.out.println("prova1");
String caso = "";
        if(Messaggio.equals("Luckizu ha richiesto di collegarsi al canale I rapaci")){
            System.out.println("prova2");
            for (int x = 0; x<120; x++){
              /* if( e.getMessage().getReactions().get()
                   x = 121;
                   e.getGuild().getDefaultChannel().sendMessage("ok").queue();
                   System.out.println("funza");

               }

            System.out.println(caso);
               System.out.println("non va");
              try {
                  Thread.sleep(1000);
              } catch (InterruptedException ex) {
                  ex.printStackTrace();
              }
            }

        } //else if() {
         //TODO aggiungere lettura per clash
*/

    }


    }

