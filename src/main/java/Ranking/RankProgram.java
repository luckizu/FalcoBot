package Ranking;

import Database.InserimentoUtenti;
import net.dv8tion.jda.api.events.guild.voice.GuildVoiceJoinEvent;
import net.dv8tion.jda.api.events.guild.voice.GuildVoiceLeaveEvent;
import net.dv8tion.jda.api.events.message.guild.GuildMessageReceivedEvent;
import net.dv8tion.jda.api.hooks.ListenerAdapter;


import java.io.IOException;

public class RankProgram extends ListenerAdapter {
    /*
    public void onGuildMessageRecieved(GuildMessageReceivedEvent event) throws IOException {

        //TODO Ricevimento messaggio, ricevimento Immagine, ricevimento video
        String Nome = event.getMember().getUser().getName();
        int[] NDATA = InserimentoUtenti.toNData(Nome);

        RankingSystem.AddM(NDATA[0], NDATA[1]);


    }

    public void onGuildVoiceJoinEvent (GuildVoiceJoinEvent event){
        //TODO Timer * punti al minuto

    }
    public void onGuildVoiceLeaveEvent (GuildVoiceLeaveEvent event){

        //TODO Blocco del timer

    }

 */
    }

